create table COCKTAIL (
                               COCKTAIL_ID UUID,
                               ID_DRINK TEXT,
                               NAME TEXT,
                               INGREDIENTS TEXT,
                               PRIMARY KEY (COCKTAIL_ID)
);